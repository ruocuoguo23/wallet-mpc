# Sign Gateway Configuration
server:
  host: "0.0.0.0"
  port: 8080
  cors_origins: ["http://localhost:3000", "http://127.0.0.1:3000"]
# Logging configuration
logging:
  level: "info"
  format: "json"  # or "text"
grpc:
  host: "0.0.0.0"
  port: 50050
sign_service:
  url: "http://host.docker.internal:50051"
